.center {
	display: flex;
	justify-content: center;
	align-items: center;
	height: 100vh;
}

.animate {
	font-size: 50px;
	animation-name: example;
	animation-duration: 2s;
	animation-iteration-count: infinite;
}

@keyframes example {
	0% {opacity: 0;}
	50% {opacity: 1;}
	100% {opacity: 0;}
}
